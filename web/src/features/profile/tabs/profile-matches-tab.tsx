import { Card, CardContent } from '@/components/ui/card';

export function ProfileMatchesTab() {
  return (
    <Card>
      <CardContent className="p-4 text-sm text-muted-foreground">
        Histórico completo de partidas em breve.
      </CardContent>
    </Card>
  );
}
