services:
  postgres:
    image: postgres:16
    container_name: arena-postgres
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: arena
      POSTGRES_PASSWORD: arena
      POSTGRES_DB: arena
    volumes:
      - arena-postgres-data:/var/lib/postgresql/data

volumes:
  arena-postgres-data:
